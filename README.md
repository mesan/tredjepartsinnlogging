# README
Simple Spring Boot app using Spark Framework to create RESTful services.

## Installation

1. `cd frontend`
2. `npm install`
3. `webpack`
4. `cd ../frontend`
5. `mvn clean package`
6. `java -jar target/app.jar`